package main

func main() {

	//Your script

	/*
		Example:


		result := NewScript("output")
		result.Append("$\n")
		defer result.WriteToFile()
		A := FromIntMatrix([][]int{
			{2, 1, 4},
			{3, -1, 0},
		})

		B := FromIntMatrix([][]int{
			{1, 3},
			{2, 2},
			{3, 1},
		})

		CFirst, _ := MulMatrix(A, B)
		CLast, _ := MulMatrix(B.T(), A.T())
		CFirst.MulMatrixNum(NewNum(3, 1))
		CLast.MulMatrixNum(NewNum(-2, 1))
		result.AddMatrixln(CFirst, "3 dot A dot B")
		result.AddMatrixln(CLast, "-2 dot B^T dot A^T")
		C, _ := AddMatrix(CFirst, CLast)
		result.AddMatrixln(C, "C")
		result.Append("$\n")
	*/

}
